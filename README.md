# SIP-review
The file `SIP Log review` has the following dependencies based on its imports:

### Python Standard Library Dependencies:
- `sys`
- `json`
- `os`
- `re`
- `logging`
- `time`
- `queue.Queue`
- `csv`
- `datetime.datetime`

### External Libraries:
1. **PyQt5**:
   - `PyQt5.QtWidgets` (e.g., `QApplication`, `QWidget`, etc.)
   - `PyQt5.QtCore` (e.g., `Qt`, `QThread`, etc.)
   - `PyQt5.QtGui` (e.g., `QColor`, `QFont`, etc.)
2. **Scapy**:
   - `scapy.all` (e.g., `rdpcap`, `IP`, `TCP`, `UDP`)
3. **FPDF**:
   - `fpdf.FPDF`

If you need more details about any of these libraries or their functions, let me know!
